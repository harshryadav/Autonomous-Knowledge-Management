# Demo Repo 1

A tiny example repository used to exercise the retrieval pipeline
end-to-end. It intentionally contains a mix of:

- A README (prose)
- A caching module with docstrings and inline comments
- A small utility module

Queries like "What does this repo do?" or "Where is caching used?"
should retrieve the relevant chunks.
